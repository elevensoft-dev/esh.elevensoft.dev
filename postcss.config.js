// Tailwind CSS 4 doesn't require PostCSS plugins
// This file is kept for compatibility but plugins are disabled
export default {
  plugins: {
    // Tailwind CSS 4 handles everything internally
    // No additional PostCSS plugins needed
  },
}; 